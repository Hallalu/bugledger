# 🔎 Scanner Findings — static-analysis leads (unverified)

Generated by `scan.mjs` across 3 app(s): **99 leads**. These are code-smell matches to verify, not confirmed bugs. Re-run `node scan.mjs <app> --write` to refresh.

## Budget LevelUp (53)

- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `listinglab/public/app.js:137` — $("status").innerHTML = (spin ? '<span class="spin"></span>' : "") +  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `listinglab/public/app.js:141` — $("fetchstatus").innerHTML = (spin ? '<span class="spin"></span>' : "") +  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **SSRF-GOTO** — SSRF / open proxy (unvalidated navigation/fetch target)  
  `listinglab/worker.js:414` — await page.goto(url, { waitUntil: "networkidle2", timeout: 40000 });  
  *Fix:* Enforce an https + host allowlist before page.goto()/fetch(); use redirect:'manual' and re-validate.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `sociallevelup/public/core.js:113` — function modal(html) { $("#modal-root").innerHTML = `<div class="overlay" onclick="if(event.target===this)closeModal()">  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `sociallevelup/public/core.js:226` — $("#nav").innerHTML = NAV.map(v => `<button class="${v === view ? "active" : ""}" data-v="${v}">${navGlyph(v)}<span>${es  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `sociallevelup/public/icons.js:109` — grid.innerHTML = list.map(([e]) => `<button class="em ${e === currentEmoji ? "on" : ""}" data-em="${e}">${e}</button>`).  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `webapp/public/app.js:758` — function modal(html) { $("#modal-root").innerHTML = `<div class="overlay" onclick="if(event.target===this)closeModal()">  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `webapp/public/auth.js:459` — $("#modal-root").innerHTML = `<div class="overlay" ${oc}><div class="modal">${html}</div></div>`;  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `webapp/public/core.js:259` — function modal(html) { $("#modal-root").innerHTML = `<div class="overlay" onclick="if(event.target===this)closeModal()">  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `webapp/public/core.js:314` — $("#nav").innerHTML = NAV.map(v => `<button class="${v === view ? "active" : ""}" data-v="${v}">${  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `webapp/public/core.js:1299` — sb.innerHTML = `${window.icon ? icon("search", 15) : "🔎"}<span data-i18n>Search</span><kbd>/</kbd>`;  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `webapp/public/icons.js:109` — grid.innerHTML = list.map(([e]) => `<button class="em ${e === currentEmoji ? "on" : ""}" data-em="${e}">${e}</button>`).  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `webapp/public/landing.js:146` — out.innerHTML = "✅ <b>Unlocked: " + esc(j.product) + "</b><br>Your code is valid. Open the app and choose <b>Restore / I  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `webapp/public/landing.js:155` — out.innerHTML = "⚠️ " + esc(msg);  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `medium` **DATE-TOISO** — Date via toISOString() can shift a day across timezones  
  `listinglab/public/app.js:68` — capturedAt: new Date().toISOString(),  
  *Fix:* For a calendar Y-M-D use local getFullYear/getMonth/getDate, not toISOString().slice(0,10).
- [ ] `medium` **DATE-TOISO** — Date via toISOString() can shift a day across timezones  
  `listinglab/public/app.js:487` — if (!launchState.started) launchState.started = new Date().toISOString();  
  *Fix:* For a calendar Y-M-D use local getFullYear/getMonth/getDate, not toISOString().slice(0,10).
- [ ] `medium` **DATE-TOISO** — Date via toISOString() can shift a day across timezones  
  `listinglab/public/app.js:488` — function saveLaunch() { launchState.updated = new Date().toISOString(); localStorage.setItem(LKEY, JSON.stringify(launch  
  *Fix:* For a calendar Y-M-D use local getFullYear/getMonth/getDate, not toISOString().slice(0,10).
- [ ] `medium` **DATE-TOISO** — Date via toISOString() can shift a day across timezones  
  `listinglab/public/app.js:522` — checks()[key] = cb.checked ? { done: true, at: new Date().toISOString() } : { done: false };  
  *Fix:* For a calendar Y-M-D use local getFullYear/getMonth/getDate, not toISOString().slice(0,10).
- [ ] `medium` **DATE-TOISO** — Date via toISOString() can shift a day across timezones  
  `listinglab/public/app.js:530` — launchState = { started: new Date().toISOString(), checks: {} }; saveLaunch(); renderLaunch();  
  *Fix:* For a calendar Y-M-D use local getFullYear/getMonth/getDate, not toISOString().slice(0,10).
- [ ] `medium` **PBKDF2-WEAK** — PBKDF2 iterations below OWASP 2026 guidance (~600k)  
  `sociallevelup/public/auth.js:21` — iterations: 250000, hash: "SHA-256" }, km, { name: "AES-GCM", length: 256 }, false, ["encrypt", "decrypt"]);  
  *Fix:* Raise iterations to >=600000; the real fix for low-entropy secrets is a retrieval token, not more rounds.
- [ ] `medium` **DATE-TOISO** — Date via toISOString() can shift a day across timezones  
  `sociallevelup/public/core.js:320` — [new Date(a.at).toISOString(), a.kind, `"${String(a.text).replace(/"/g, '""')}"`,  
  *Fix:* For a calendar Y-M-D use local getFullYear/getMonth/getDate, not toISOString().slice(0,10).
- [ ] `medium` **PBKDF2-WEAK** — PBKDF2 iterations below OWASP 2026 guidance (~600k)  
  `sociallevelup/public/core.js:452` — iterations: 100000, hash: "SHA-256" }, km, { name: "AES-GCM", length: 256 }, false, ["encrypt", "decrypt"]);  
  *Fix:* Raise iterations to >=600000; the real fix for low-entropy secrets is a retrieval token, not more rounds.
- [ ] `medium` **FIND-DEREF** — Property access directly on a .find()/.match() result (may be undefined)  
  `sociallevelup/public/core.js:582` — <div class="sync-code" style="display:block;text-align:center;font-size:15px;margin:10px 0">${secret.match(/.{1,4}/g).jo  
  *Fix:* Guard the result before dereferencing to avoid 'cannot read property of undefined'.
- [ ] `medium` **DATE-TOISO** — Date via toISOString() can shift a day across timezones  
  `webapp/public/activity.js:64` — [new Date(a.at).toISOString(), a.kind, `"${String(a.text).replace(/"/g, '""')}"`,  
  *Fix:* For a calendar Y-M-D use local getFullYear/getMonth/getDate, not toISOString().slice(0,10).
- [ ] `medium` **DATE-TOISO** — Date via toISOString() can shift a day across timezones  
  `webapp/public/app.js:6` — const todayISO = () => new Date().toISOString().slice(0, 10);  
  *Fix:* For a calendar Y-M-D use local getFullYear/getMonth/getDate, not toISOString().slice(0,10).
- [ ] `medium` **DATE-TOISO** — Date via toISOString() can shift a day across timezones  
  `webapp/public/app.js:12` — const iso = d => d.toISOString().slice(0, 10);  
  *Fix:* For a calendar Y-M-D use local getFullYear/getMonth/getDate, not toISOString().slice(0,10).
- [ ] `medium` **PBKDF2-WEAK** — PBKDF2 iterations below OWASP 2026 guidance (~600k)  
  `webapp/public/auth.js:43` — iterations: 250000, hash: "SHA-256" }, km, { name: "AES-GCM", length: 256 }, false, ["encrypt", "decrypt"]);  
  *Fix:* Raise iterations to >=600000; the real fix for low-entropy secrets is a retrieval token, not more rounds.
- [ ] `medium` **FIND-DEREF** — Property access directly on a .find()/.match() result (may be undefined)  
  `webapp/public/auth.js:469` — <div class="sync-code" style="display:block;text-align:center;font-size:15px;margin:10px 0">${secret.match(/.{1,4}/g).jo  
  *Fix:* Guard the result before dereferencing to avoid 'cannot read property of undefined'.
- [ ] `medium` **PBKDF2-WEAK** — PBKDF2 iterations below OWASP 2026 guidance (~600k)  
  `webapp/public/core.js:941` — iterations: 100000, hash: "SHA-256" }, km, { name: "AES-GCM", length: 256 }, false, ["encrypt", "decrypt"]);  
  *Fix:* Raise iterations to >=600000; the real fix for low-entropy secrets is a retrieval token, not more rounds.
- [ ] `medium` **FIND-DEREF** — Property access directly on a .find()/.match() result (may be undefined)  
  `webapp/public/core.js:1073` — <div class="sync-code" style="display:block;text-align:center;font-size:15px;margin:10px 0">${secret.match(/.{1,4}/g).jo  
  *Fix:* Guard the result before dereferencing to avoid 'cannot read property of undefined'.
- [ ] `medium` **DATE-TOISO** — Date via toISOString() can shift a day across timezones  
  `webapp/public/life.js:11` — for (;;) { const iso = d.toISOString().slice(0, 10);  
  *Fix:* For a calendar Y-M-D use local getFullYear/getMonth/getDate, not toISOString().slice(0,10).
- [ ] `medium` **DUP-DOM-ID** — Duplicate DOM id in markup (breaks getElementById/handlers)  
  `webapp/public/pitch.html:185` — id="price" appears 2× (lines 166, 185)  
  *Fix:* Make ids unique; duplicate ids silently wire events to the wrong element.
- [ ] `medium` **FIND-DEREF** — Property access directly on a .find()/.match() result (may be undefined)  
  `webapp/public/setup.js:141` — const f = CURRENCIES.find(c => c[0] === el.querySelector("#su-currency").value);  
  *Fix:* Guard the result before dereferencing to avoid 'cannot read property of undefined'.
- [ ] `medium` **FIND-DEREF** — Property access directly on a .find()/.match() result (may be undefined)  
  `webapp/worker.js:292` — return "BLU-" + b32encode(full).match(/.{1,4}/g).join("-");  
  *Fix:* Guard the result before dereferencing to avoid 'cannot read property of undefined'.
- [ ] `medium` **FIND-DEREF** — Property access directly on a .find()/.match() result (may be undefined)  
  `webapp/worker.js:305` — return { ok: true, idx, product: PRODUCT_BY_IDX[idx], code: "BLU-" + s.match(/.{1,4}/g).join("-") };  
  *Fix:* Guard the result before dereferencing to avoid 'cannot read property of undefined'.
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `listinglab/public/app.js:77` — ".then(function(){alert('Listing Lab: captured \\u201c'+((d.title||'').slice(0,50))+'\\u201d with '+d.images.length+' im  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `listinglab/public/app.js:79` — "else{window.prompt('Copy this:',s)}}catch(e){alert('Listing Lab could not read this page: '+e.message)}})()";  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NO-CSP** — No Content-Security-Policy found in served HTML  
  `listinglab/public/index.html:1` — index.html  
  *Fix:* Add a CSP (meta or worker response header) to reduce XSS blast radius.
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `sociallevelup/public/core.js:333` — r.onload = () => { try { S = normalize(JSON.parse(r.result)); render(); } catch (e) { alert("That didn't look like a Soc  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `sociallevelup/public/core.js:474` — } catch (e) { if (showResult) alert("Backup didn't go through: " + e.message); }  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `sociallevelup/public/core.js:479` — if (r.status === 404) return alert("No backup found for that code — check the spelling (it matters exactly).");  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `sociallevelup/public/core.js:489` — } catch (e) { alert("Couldn't restore: wrong code, or " + e.message); }  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NO-CSP** — No Content-Security-Policy found in served HTML  
  `sociallevelup/public/index.html:1` — index.html  
  *Fix:* Add a CSP (meta or worker response header) to reduce XSS blast radius.
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `webapp/public/app.js:548` — on("#tx-add", "onclick", () => { const a = num($("#tx-amount").value); if (!a) return alert("Enter an amount");  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `webapp/public/app.js:614` — r.onerror = () => { closeModal(); alert("Didn't catch that — try again."); };  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `webapp/public/app.js:752` — r.onload = () => { try { S = JSON.parse(r.result); render(); } catch (e) { alert("That file didn't look like a Budget St  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `webapp/public/core.js:767` — r.onload = () => { try { S = JSON.parse(r.result); render(); } catch (e) { alert("That didn't look like a LevelUp backup  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `webapp/public/core.js:963` — } catch (e) { if (showResult) alert("Backup didn't go through: " + e.message); }  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `webapp/public/core.js:968` — if (r.status === 404) return alert("No backup found for that code — check the spelling (it matters exactly).");  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `webapp/public/core.js:978` — } catch (e) { alert("Couldn't restore: wrong code, or " + e.message); }  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NO-CSP** — No Content-Security-Policy found in served HTML  
  `webapp/public/index.html:1` — index.html  
  *Fix:* Add a CSP (meta or worker response header) to reduce XSS blast radius.
- [ ] `low` **NO-CSP** — No Content-Security-Policy found in served HTML  
  `webapp/public/landing.html:1` — landing.html  
  *Fix:* Add a CSP (meta or worker response header) to reduce XSS blast radius.
- [ ] `low` **NO-CSP** — No Content-Security-Policy found in served HTML  
  `webapp/public/pitch.html:1` — pitch.html  
  *Fix:* Add a CSP (meta or worker response header) to reduce XSS blast radius.

## Stitchhooky (36)

- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:426` — function toast(msg,icon){const t=$('toast');t.innerHTML=(icon?'<svg class="ic"><use href="#'+icon+'"/></svg>':'')+'<span  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:602` — card.innerHTML=banner+visual+'<span class="info"><h3>'+esc(p.name)+'</h3><span class="meta">'+p.rows.length+' rows \u00b  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:659` — row.innerHTML=draft.banner.map((u,i)=>'<span class="bn"><img src="'+u+'"><button data-bx="'+i+'">✕</button></span>').joi  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:689` — el.innerHTML='<span class="rn">'+(i+1)+'</span><span class="cells">'+r.cells.map(c=>{  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:714` — else{s.innerHTML='<input type="color" value="'+colorOfDraft(k)+'" data-k="'+k+'"> <span class="ln" style="cursor:pointer  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:913` — box.innerHTML='<b>Pattern pages:</b> <span style="display:inline-flex;gap:8px;vertical-align:middle;margin-left:6px">'+  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:919` — $('inspWatch').innerHTML=ins.length?('<div class="tipbox" style="display:block"><b>Watch along</b>'+ins.map(it=>  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:958` — if(img)ringEl.innerHTML='<img src="'+img+'" alt="project"><span class="pctlab">'+pct+'%</span>';  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:959` — else ringEl.innerHTML='<span class="ph">🧶</span><span class="pctlab">'+pct+'%</span>';  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1035` — $('invoicePaper').innerHTML='<div class="inv-paper" style="max-height:60vh;overflow-y:auto">'+  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1045` — if(nxt){$('hudNext').innerHTML='Next up: <b id="nextLink">'+nxt.parts.map(p=>dispName(p)).join(' + ')+'</b>';  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1122` — body.innerHTML='<div id="sessLive">'+liveSessionHTML()+'</div>'+done+  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1317` — if(note){gn.style.display='block';gn.innerHTML='<b>Pattern says:</b> '+esc(note)}else gn.style.display='none';  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1324` — $('guideSteps').innerHTML=g.steps.map((s,i)=>'<div class="gstep"><span class="num">'+(i+1)+'</span><p>'+s+'</p></div>').  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1325` — $('guideTip').innerHTML='<b>Tip ✿</b> '+g.tip;  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1529` — ringEl.innerHTML=(db.user.avatar?'<img src="'+esc(db.user.avatar)+'" alt="me">':'<span class="ph">🧶</span>');  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1604` — t.innerHTML='<span class="dot"></span>'+({live:'listening — say “one”, “done three”, “undo”…',  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1819` — $('streakFreeze').innerHTML=st.freezes?('<span class="freezepill"><svg class="ic" style="width:11px;height:11px"><use hr  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1833` — $('invoicePaper').innerHTML='<div class="inv-paper" style="max-height:64vh;overflow-y:auto">'+  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1861` — row.innerHTML=swatches.map(c=>'<button data-col="'+c+'" class="'+(p.accent===c?'on':'')+'" style="background:'+c+'"></bu  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1874` — w.innerHTML='<button class="dr-item" data-t="">'+  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1886` — wrap.innerHTML='<button class="dotsbtn" title="Project options"><svg class="ic"><use href="#i-dots"/></svg></button>'+  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1963` — const ringEl=$('profRing');ringEl.innerHTML=(r.avatar?'<img src="'+esc(r.avatar)+'" alt="">':(db.user.avatar&&r.isMe?'<i  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1995` — if(!list||!list.length){el.innerHTML='<p class="hint">No one here yet — share the code “'+esc(this.room)+'” with a frien  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/app/app.js:1996` — el.innerHTML=list.map(p=>'<div style="display:flex;gap:12px;align-items:center;padding:10px 0;border-bottom:1px solid va  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `medium` **FIND-DEREF** — Property access directly on a .find()/.match() result (may be undefined)  
  `public/app/app.js:3559` — const hit=accts.find(a=>who===(a.user.handle||'').toLowerCase()||who===(a.user.email||'').toLowerCase());  
  *Fix:* Guard the result before dereferencing to avoid 'cannot read property of undefined'.
- [ ] `medium` **FIND-DEREF** — Property access directly on a .find()/.match() result (may be undefined)  
  `public/app/app.js:3570` — const hit=accts.find(a=>who===(a.user.handle||'').toLowerCase()||who===(a.user.email||'').toLowerCase())  
  *Fix:* Guard the result before dereferencing to avoid 'cannot read property of undefined'.
- [ ] `medium` **PBKDF2-WEAK** — PBKDF2 iterations below OWASP 2026 guidance (~600k)  
  `src/worker.js:120` — { name: "PBKDF2", hash: "SHA-256", salt, iterations: 100000 }, key, 256);  
  *Fix:* Raise iterations to >=600000; the real fix for low-entropy secrets is a retrieval token, not more rounds.
- [ ] `medium` **DATE-TOISO** — Date via toISOString() can shift a day across timezones  
  `src/worker.js:631` — number: o.number, date: new Date((o.created || 0) * 1000).toISOString().slice(0, 10),  
  *Fix:* For a calendar Y-M-D use local getFullYear/getMonth/getDate, not toISOString().slice(0,10).
- [ ] `low` **NO-CSP** — No Content-Security-Policy found in served HTML  
  `docs/screens/android-mock.html:1` — android-mock.html  
  *Fix:* Add a CSP (meta or worker response header) to reduce XSS blast radius.
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `public/app/app.js:1904` — const n=prompt('Rename project',p.name);  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `public/app/app.js:3020` — const n=prompt('Name this chart',cur_);  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NATIVE-DIALOG** — Native browser dialog used for UX  
  `public/app/app.js:3776` — const bp=prompt('Enter your backup password to unlock');  
  *Fix:* Replace prompt()/alert()/confirm() with in-app UI (native dialogs look broken and block).
- [ ] `low` **NO-CSP** — No Content-Security-Policy found in served HTML  
  `public/app/index.html:1` — index.html  
  *Fix:* Add a CSP (meta or worker response header) to reduce XSS blast radius.
- [ ] `low` **NO-CSP** — No Content-Security-Policy found in served HTML  
  `public/index.html:1` — index.html  
  *Fix:* Add a CSP (meta or worker response header) to reduce XSS blast radius.
- [ ] `low` **NO-CSP** — No Content-Security-Policy found in served HTML  
  `public/pitch/index.html:1` — index.html  
  *Fix:* Add a CSP (meta or worker response header) to reduce XSS blast radius.

## Breadcrumb (10)

- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/capture.js:383` — list.innerHTML = `<div class="it sel" data-capture="1">${IC.plus}<span>Keep “${esc(input.value.trim())}” as a prompt</sp  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/game.js:145` — el.innerHTML = `<b>${esc(what)}</b>${detail ? `<span>${esc(detail)}</span>` : ''}`;  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/signin.js:366` — if (av) { av.style.overflow = 'hidden'; av.innerHTML = `<img class="ava-img" src="${data}" alt="">`; }  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/ui.js:91` — el.className = 'toast'; el.setAttribute('role', 'status'); el.innerHTML = IC.check + esc(msg);  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/ui.js:122` — dr.innerHTML = `<div class="drawer-body">${html}</div>`;  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `high` **XSS-INNERHTML** — Stored/DOM XSS (HTML sink with interpolation)  
  `public/views3.js:193` — host.innerHTML = `<div style="font-size:14.5px;line-height:1.65">${esc(r.text)}</div>  
  *Fix:* Ensure every interpolated value is escaped (an esc() that also handles " and '), or build DOM with textContent.
- [ ] `medium` **FIND-DEREF** — Property access directly on a .find()/.match() result (may be undefined)  
  `public/parse.js:61` — || S.features.find(x => x.project === pid && x.name.toLowerCase().startsWith(n));  
  *Fix:* Guard the result before dereferencing to avoid 'cannot read property of undefined'.
- [ ] `medium` **FIND-DEREF** — Property access directly on a .find()/.match() result (may be undefined)  
  `public/parse.js:158` — t = t.split('\n').find(l => l.trim().length > 1) || t;  
  *Fix:* Guard the result before dereferencing to avoid 'cannot read property of undefined'.
- [ ] `medium` **DATE-TOISO** — Date via toISOString() can shift a day across timezones  
  `public/views3.js:289` — exported: new Date().toISOString(),  
  *Fix:* For a calendar Y-M-D use local getFullYear/getMonth/getDate, not toISOString().slice(0,10).
- [ ] `low` **NO-CSP** — No Content-Security-Policy found in served HTML  
  `public/index.html:1` — index.html  
  *Fix:* Add a CSP (meta or worker response header) to reduce XSS blast radius.
